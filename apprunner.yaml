version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - echo "Build complete"
run:
  command: npm start
  network:
    port: 3000
    env: PORT
  env:
    - name: PORT
      value: "3000"
